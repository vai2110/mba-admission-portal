name: MNNIT Executive MBA Fee Fix v2

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-fees:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Fee update completed; workflow retained for manual dispatch only."
